# 1. Use the absolute smallest Linux image available (around 5MB)
FROM alpine:3.19

# 2. Add an environment variable to identify it
ENV APP_STAGE="Infrastructure Mock Test"

# 3. Simply echo a message and keep the container alive for testing
CMD ["sh", "-c", "echo 'IoT Telemetry Ingestor Platform Mock Running...' && sleep infinity"]